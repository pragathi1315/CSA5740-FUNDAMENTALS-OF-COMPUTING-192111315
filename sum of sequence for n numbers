#include<stdio.h>
int main()
{
	int i, N, sum;
	 printf("enter the value of the n:");
	 scanf("%d", &N);
	 printf("\n");
	 sum=0;
	 for(i=0; i<=N; i++)
	 sum=sum+i;
	 {
	    printf("%d\n",sum);
	 }
	 
	return 0;
}
